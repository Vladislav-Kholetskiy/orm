
# ORM Learning Platform

Учебная платформа для управления онлайн-курсами, заданиями и тестами.  
Проект показывает использование **Spring Boot + Spring Data JPA + PostgreSQL** с полноценной моделью данных, REST API и тестами (unit + integration).


---

## Описание

Платформа моделирует работу учебного сервиса:

- управление курсами, модулями и уроками;
- запись студентов на курсы;
- задания и решения;
- тесты/викторины;
- уведомления;
- отзывы о курсах.

Проект сфокусирован на **ORM-модели** и корректной работе **CRUD-операций** через Spring Data JPA.

---

## Функциональность

1. **Пользователи и роль**: STUDENT / TEACHER / ADMIN.
2. **Курсы**:
   - принадлежность категории и преподавателю,
   - структура: `Course → Module → Lesson`,
   - теги и отзывы.
3. **Запись на курс**:
   - Many-to-Many через сущность `Enrollment`,
   - статус записи (ACTIVE / COMPLETED / CANCELLED),
   - запрет двойной записи на один курс.
4. **Задания и решения**:
   - `Assignment` привязана к уроку,
   - `Submission` — решение студента,
   - запрет повторной отправки,
   - оценивание и статусы решений.
5. **Тесты/викторины**:
   - `Quiz`, `Question`, `AnswerOption`, `QuizSubmission`,
   - прохождение теста, сохранение результатов.
6. **Уведомления для пользователей**.
7. **REST API** для основных операций.
8. **Предзаполнение демоданными** при старте.
9. **Unit + integration tests**.

---

## Технологии

- **Java** 23
- **Spring Boot** 3.3.x
- **Spring Web (REST)**
- **Spring Data JPA / Hibernate**
- **PostgreSQL** (основная БД)
- **H2** (для тестов, при необходимости)
- **Testcontainers** (интеграционные тесты с реальным PostgreSQL)
- **Lombok**
- **JUnit 5, AssertJ, Spring Boot Test**

---

## Модель данных

Основные сущности (15+):

- `User` (роль: STUDENT / TEACHER / ADMIN)
- `Profile` (1–1 с пользователем)
- `Category`
- `Course` (многие курсы → 1 категория, 1 преподаватель)
- `Module` (1–M в рамках курса)
- `Lesson` (1–M в рамках модуля)
- `LessonResource` (материалы урока: FILE / LINK / NOTE)
- `Enrollment` (Many-to-Many: студент ↔ курс со статусом)
- `Assignment` (задание к уроку)
- `Submission` (решение задания студентом)
- `Tag` (Many-to-Many к курсам)
- `CourseReview` (отзыв студента на курс)
- `Notification` (уведомления пользователю)
- `Quiz` (тест по модулю)
- `Question` (вопрос теста)
- `AnswerOption` (варианты ответа)
- `QuizSubmission` (прохождение теста студентом)

Связи:

- 1–1: `User` ↔ `Profile`
- 1–M: `Course` → `Module` → `Lesson` → `Assignment` / `LessonResource`
- M–M: `Course` ↔ `Tag` через таблицу `course_tag`
- M–M: `User` (STUDENT) ↔ `Course` через `Enrollment`
- Доп. связи: `CourseReview`, `QuizSubmission`, `Notification` и др.

---

## Структура проекта

Пакеты:

```text
orm
 ├─ OrmApplication.java        # точка входа
 ├─ config/                    # конфигурация (профили, тестовые настройки)
 ├─ entity/                    # JPA-сущности
 ├─ model/                     # enum'ы (Role, CourseStatus, ...)
 ├─ repository/                # Spring Data репозитории
 ├─ service/                   # сервисные интерфейсы
 │   └─ impl/                  # реализация сервисов
 ├─ controller/                # REST-контроллеры
 ├─ exception/                 # глобальный обработчик ошибок
 ├─ config/                    # предзаполнение БД (DataInitializer)
 └─ it/, service/.../test      # интеграционные и unit тесты
```
# Настройка и запуск

## PostgreSQL

1. Установить PostgreSQL и запомнить логин/пароль суперпользователя.

2. Создать БД и пользователя (пример):

```sql
CREATE DATABASE orm_db;
CREATE USER orm WITH PASSWORD 'orm_password';
GRANT ALL PRIVILEGES ON DATABASE orm_db TO orm;
```
3. В файле src/main/resources/application-dev.yml указать свои настройки:
```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/orm_db
    username: orm
    password: orm_password
  jpa:
    hibernate:
      ddl-auto: update  
    show-sql: true
    properties:
      hibernate.format_sql: true
```
## Профили Spring

Используются профили:

- **dev** — основная разработка (PostgreSQL, демо-данные)
- **test** — тестовый профиль (H2 или Testcontainers)

## Активация профиля

Можно задать активный профиль:

через application.yml / application-dev.yml;

##  Запуск приложения

### 1. Сборка проекта
Из корня проекта выполните:

```bash
mvn clean package
```

##  2. Запуск приложения

Запуск с активным профилем **dev**:

```bash
mvn spring-boot:run -Dspring-boot.run.profiles=dev
```

##  3. Доступ к приложению

После запуска приложение доступно по адресу:

 http://localhost:8080

---

##  4. Быстрая проверка API

Базовые эндпоинты можно проверить с помощью:

-  браузера  
-  Postman  
-  любого REST-клиента  

## REST API — основные эндпоинты

Ниже приведён укороченный список эндпоинтов (без всех DTO и детальных примеров).

---

## 👤 Пользователи

| Метод | Эндпоинт               | Описание                |
|-------|-------------------------|--------------------------|
| GET   | `/api/users`            | Список пользователей     |
| GET   | `/api/users/{id}`       | Получить пользователя    |
| POST  | `/api/users`            | Создать пользователя     |
| PUT   | `/api/users/{id}`       | Обновить пользователя    |
| DELETE| `/api/users/{id}`       | Удалить пользователя     |

---

## 📚 Курсы и структура

| Метод | Эндпоинт                        | Описание                                |
|-------|----------------------------------|------------------------------------------|
| GET   | `/api/courses`                   | Список курсов                            |
| GET   | `/api/courses/{id}`              | Курс без структуры                       |
| GET   | `/api/courses/{id}/full`         | Курс с модулями и уроками               |
| POST  | `/api/courses`                   | Создать курс (`teacherId`, `categoryId`) |
| PUT   | `/api/courses/{id}`              | Обновить курс                            |
| DELETE| `/api/courses/{id}`              | Удалить курс                             |
| POST  | `/api/courses/{id}/modules`      | Добавить модуль в курс                   |
| POST  | `/api/modules/{id}/lessons`      | Добавить урок в модуль                   |

---

## 🎓 Запись на курс

| Метод | Эндпоинт                                 | Описание                     |
|-------|-------------------------------------------|-------------------------------|
| POST  | `/api/enrollments`                        | Записать студента на курс     |
| DELETE| `/api/enrollments/{id}`                   | Отменить запись               |
| GET   | `/api/enrollments/student/{studentId}`    | Курсы, на которые записан студент |
| GET   | `/api/enrollments/course/{courseId}`      | Студенты курса                |

---

## 📝 Задания и решения

| Метод | Эндпоинт                                              | Описание                          |
|-------|--------------------------------------------------------|------------------------------------|
| POST  | `/api/lessons/{lessonId}/assignments`                 | Создать задание                    |
| GET   | `/api/lessons/{lessonId}/assignments`                 | Список заданий по уроку            |
| POST  | `/api/assignments/{assignmentId}/submit`              | Студент отправляет решение         |
| POST  | `/api/submissions/{submissionId}/grade`               | Преподаватель ставит оценку        |

---

## 🧪 Тесты / Викторины

| Метод | Эндпоинт                               | Описание                     |
|-------|-----------------------------------------|-------------------------------|
| POST  | `/api/modules/{moduleId}/quizzes`       | Создать викторину            |
| GET   | `/api/quizzes/{id}`                     | Получить викторину (с вопросами) |
| POST  | `/api/quizzes/{quizId}/submit`          | Пройти тест (QuizSubmission) |

---

## 📦 Предзаполнение демо-данными (dev профиль)

При запуске приложения в профиле **dev** автоматически выполняется компонент  
`DataInitializer`, который создает демонстрационные данные для быстрого старта.

### Что создаётся при старте:

- **Пользователи**
  - 1 преподаватель (**ROLE_TEACHER**)
  - 2 студента (**ROLE_STUDENT**)

- **Категории курсов** (несколько штук)

- **Демонстрационный курс**, включающий:
  - 1 модуль
  - 1 урок
  - учебные ресурсы
  - задание (Assignment)

- **Записи на курс**
  - оба студента автоматически записываются на демонстрационный курс

- **Викторина (Quiz)** с:
  - вопросами
  - вариантами ответов

- **Примерные данные по активности студентов**:
  - решения заданий (**Submission**)
  - сдача теста (**QuizSubmission**)

###  Что это даёт

Сразу после запуска приложение полностью готово к демонстрации:  
можно выполнять запросы к API, тестировать UI или интеграции — уже с заполненной рабочей БД.


